# Application workspace

The project overview, architecture, provider setup, and contribution notes live in the [repository README](../README.md).

## Commands

```bash
npm install
npm run dev
npm run test:engine
npm run lint
npx tsc --noEmit
npm run build
```

The Next.js application runs at [http://localhost:3000](http://localhost:3000).
